.git
.git/**
.env
.env.local

node_modules
**/node_modules
**/dist

logs
logs/**
data
data/**

build
build/**

# Host Rust build output: huge, and the wrong arch/OS for the in-image build
# (the demo Dockerfile compiles binaries fresh from source). Keep it out of the
# build context.
**/target
**/.tmp
