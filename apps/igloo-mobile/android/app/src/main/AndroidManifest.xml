<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
        Camera permission for the QR scanner affordance on the Onboard Device
        flow (mobile-qr-display-and-scan-support, VAL-QR-002/003). Declared
        optional via `required="false"` so the app installs on emulator AVDs
        that do not provide a back camera. When the device reports no camera
        we surface the camera-unavailable fallback screen with a paste
        affordance.
    -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:allowBackup="true"
        android:label="Igloo Mobile"
        android:supportsRtl="true"
        android:theme="@style/Theme.App">

        <activity
            android:name="com.frostr.igloo.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
